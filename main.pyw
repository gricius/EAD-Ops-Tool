# main.pyw the same as main.py but with a different extension to disable the console window.


# Application EAD Ops Tool entry point
from gui import create_main_window

if __name__ == "__main__":
    create_main_window()
